package fr.dawoox.yua.modules;

import discord4j.common.util.Snowflake;
import discord4j.core.object.entity.Guild;
import discord4j.core.object.entity.Member;
import fr.dawoox.yua.utils.LogsManager;

public class ModuleManager {
/*
    public static boolean isActivate(String module, Guild guild, Member sender){

        Snowflake guildId = guild.getId();

        switch (module){
            case "gifs":

                break;

            case "test":

                break;

            default:
                LogsManager.logAction("MAJOR INFO : THIS MODULE DO NOT EXIST", sender, ModuleManager.class);
                return false;
        }

        return false;
    }*/

}
